server:
  port: 8080

app:
  prompts:
    directory: ${PROMPT_DIR:prompts}

spring:
  ai:
    openai:
      api-key: ${ZHIPU_API_KEY:not-configured}
      base-url: https://open.bigmodel.cn
      chat:
        completions-path: /api/paas/v4/chat/completions
        options:
          model: glm-4-flash
          temperature: 0.7
