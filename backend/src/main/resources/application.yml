server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${ZHIPU_API_KEY}
      base-url: https://open.bigmodel.cn
      chat:
        completions-path: /api/paas/v4/chat/completions
        options:
          model: glm-4-flash
          temperature: 0.7
